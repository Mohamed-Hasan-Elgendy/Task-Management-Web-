@model IEnumerable<myprojects>
@{
}
<h1>Project Management</h1>
<hr />

<a class="btn btn-primary" asp-action="Add" asp-controller="MYPROJECTS">Add New Project</a>
</p>
<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Project Name</th>
            <th>Project Description</th>
            <th>Project Startdate</th>
            <th>Project Enddate</th>
            <th>Project's Tasks</th>          
            <th>Options</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>@item.Startdate</td>
                <td>@item.Enddate</td>
                <td>@item.Mytasks.</td>
             
                <td>
                    <a class="btn btn-primary" asp-controller="MYPROJECTS" asp-action="Update" asp-route-id="@item.Id">Update</a>
                    <a class="btn btn-danger" asp-controller="MYPROJECTS" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>